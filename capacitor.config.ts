import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'trad-assist-ui',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
